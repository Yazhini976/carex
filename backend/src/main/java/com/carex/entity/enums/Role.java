package com.carex.entity.enums;

/**
 * Represents the role of a user within the CAREX system.
 * Stored as a VARCHAR string in the {@code users.role} column.
 */
public enum Role {
    PATIENT,
    DOCTOR,
    ADMIN
}
